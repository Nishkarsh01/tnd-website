import React from 'react';
import styles from './Events.module.css';

const Events = () => (
  <div className={styles.mainContent}>
    <h2>Events</h2>
    <h3>There are no upcoming events currently scheduled...</h3>
  </div>
);

export default Events;
